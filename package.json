{
  "name": "beacon-project-os",
  "version": "0.2.0",
  "private": true,
  "description": "Local-first Project Navigation OS",
  "repository": {
    "type": "git",
    "url": "https://github.com/dayainow/beacon-project-os.git"
  },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "verify:package": "node scripts/verify-package.mjs",
    "verify": "pnpm build && pnpm lint && pnpm test && pnpm verify:package"
  },
  "devDependencies": {
    "@types/node": "22.15.3",
    "esbuild": "0.28.1",
    "turbo": "2.10.4",
    "typescript": "5.8.3"
  },
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=22.13"
  }
}
